# CodeEcho Environment Configuration
# Copy this file to .env and update the values as needed

# Database Configuration
# Note: Update passwords in production
MYSQL_ROOT_PASSWORD=change_me_root_password
MYSQL_DATABASE=codeecho_db
MYSQL_USER=codeecho_user
MYSQL_PASSWORD=change_me_user_password
DB_DSN=codeecho_user:change_me_user_password@tcp(127.0.0.1:3306)/codeecho_db?parseTime=true

# JWT Configuration
# IMPORTANT: Generate a strong, random JWT secret for production
# You can use: openssl rand -base64 32
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_SECRET_FOR_PRODUCTION
JWT_EXPIRATION_HOURS=24

# API Configuration
# Ports for the API and UI services
API_PORT=8080
UI_PORT=3000

# Docker Configuration
# Project name used by Docker Compose
COMPOSE_PROJECT_NAME=codeecho

# Development Notes:
# 1. Copy this file: cp .env.example .env
# 2. Update database passwords (MYSQL_ROOT_PASSWORD, MYSQL_PASSWORD, DB_DSN)
# 3. Generate a secure JWT_SECRET for production
# 4. Adjust ports if needed (API_PORT, UI_PORT)
# 5. Run: make start